#!/bin/bash
# OnBuzz Community - Linux Pre-removal Script

set -e

BIN_DIR="/usr/bin"

# Remove symlink
if [ -L "$BIN_DIR/onbuzz" ]; then
    rm -f "$BIN_DIR/onbuzz"
fi

echo "OnBuzz Community has been removed."

exit 0
